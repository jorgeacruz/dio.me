import { TransformatorPipe } from './transformator-pipe';

describe('TransformatorPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformatorPipe();
    expect(pipe).toBeTruthy();
  });
});
